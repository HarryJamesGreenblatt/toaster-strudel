# Personal WIP — not for publishing
/jams/
/arrangements/

# Local-scope agent knowledge + per-track working data (durable, not published).
# Point agents here across sessions: read /context/AGENTS.md first.
/context/
/artifacts/

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Downloaded / captured media — COPYRIGHT: never publish
*.mid
*.midi
*.wav
*.flac
*.mp3
*.m4a
*.ogg
/data/
/captures/

# Python MIR sidecar
mir/.venv/
**/__pycache__/
*.pyc
*.pyo
.python-version

# Secrets — NEVER commit. Real keys live in Windows Credential Manager (keyring).
.env
mir/.env

# OS / editor cruft
.DS_Store
Thumbs.db

# copilot workspaces
*.code-workspace